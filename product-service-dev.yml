spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://postgres:5432/shopdb
    username: postgres
    password: '{cipher}a10b1168c43878e87609847f67f41ef475c05da90b814ec55aa81c4d5718ae49test'
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

app:
  message: "Hello from DEV config"

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info,env
